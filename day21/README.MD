# Day21 - Goal

## Main Goal
Add configuration support to control the todo storage path.

## Learning Objectives
1. Read config from a JSON file safely.
2. Provide a default when config is missing.
3. Keep tests passing while adding config support.

## Done Criteria
- `python -m pytest -q day09` passes
- Config loader returns a path or default
